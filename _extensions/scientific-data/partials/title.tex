$if(title)$
\title{$title$}
$endif$

$for(by-author)$
  \author[$for(it.affiliations/first)$$it.name$$endfor$]{$by-author.name.literal$}
$endfor$

$for(authornotes)$
\affil[$authornotes.note$]{$authornotes.text$}
$endfor$
